{
  "name": "jira-test-automation",
  "version": "1.0.0",
  "description": "Real browser automation for JIRA Test Generator",
  "main": "index.js",
  "scripts": {
    "test": "node test-runner.js",
    "install-deps": "npm install",
    "setup": "npm install && node setup.js"
  },
  "dependencies": {
    "puppeteer": "^21.0.0",
    "selenium-webdriver": "^4.15.0",
    "chromedriver": "^118.0.0"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
